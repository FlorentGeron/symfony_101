{% extends 'base.html.twig' %}

{% block title %}New Channel{% endblock %}

{% block body %}
<div class="container">
  <div class="lonely-block">
    <h1>Create new Channel</h1>

    {{ include('channel/_form.html.twig') }}

    <a href="{{ path('app_channel_index') }}">back to list</a>
  </div>
</div>
{% endblock %}
