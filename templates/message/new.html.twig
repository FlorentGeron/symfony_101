{% extends 'base.html.twig' %}

{% block title %}New Message{% endblock %}

{% block body %}
  <div class="container">
    <div class="lonely-block">
    <h1>Create new Private Message</h1>

    {{ include('message/_form.html.twig') }}

    <a href="{{ path('app_message_index') }}">back to list</a>
    </div>
  </div>
{% endblock %}
