{{ form_start(form) }}
  <div class="inline-form-msg">
    {{ form_widget(form.Content) }}
    <button class="bacchus-button">{{ button_label|default('Send') }}</button>
  </div>
{{ form_end(form) }}
